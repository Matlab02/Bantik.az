import{ProductForm}from"@/components/admin/product-form";import{db}from"@/lib/db";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,product=await db?.product.findUnique({where:{slug:id},include:{inventory:{include:{branch:true}},variants:true}});return<><div className="admin-head"><div><span>KATALOQ</span><h1>Məhsulu redaktə et</h1></div></div><ProductForm id={id}/>{product&&<section className="product-inventory"><span>REAL-TIME INVENTORY</span><h2>Filial stokları</h2>{product.inventory.map(row=><article key={row.id}><b>{row.branch.name}</b><span>Physical: {row.quantity}</span><span>Reserved: {row.reservedQuantity}</span><span>Available: {row.quantity-row.reservedQuantity}</span></article>)}</section>}</>}
