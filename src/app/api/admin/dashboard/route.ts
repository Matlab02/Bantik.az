import{NextResponse}from"next/server";import{getDashboard}from"@/lib/management";import{authError,requireStaff}from"@/lib/rbac";export const dynamic="force-dynamic";export async function GET(request:Request){try{const staff=await requireStaff();return NextResponse.json(await getDashboard(new URL(request.url).searchParams,staff),{headers:{"cache-control":"no-store"}})}catch(error){const e=authError(error);return NextResponse.json({error:e.status===400?"Dashboard məlumatları yüklənmədi":e.message},{status:e.status})}}
