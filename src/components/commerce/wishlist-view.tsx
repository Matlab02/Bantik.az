"use client";import Link from "next/link";import {Heart} from "lucide-react";import {products} from "@/lib/catalog";import {useStore} from "./store-provider";import {ProductCard} from "./product-card";
export function WishlistView(){const {wishlist}=useStore(),items=products.filter(p=>wishlist.includes(p.id));return <main className="account-list container"><div className="breadcrumbs">Ana səhifə / Seçilmişlər</div><div className="catalog-hero"><span>SƏNİN SİYAHIN</span><h1>Seçilmişlər</h1></div>{items.length?<div className="catalog-grid">{items.map(p=><ProductCard product={p} key={p.id}/>)}</div>:<div className="empty-state"><Heart/><h2>Seçilmiş məhsul yoxdur</h2><p>Bəyəndiyin məhsulları ürək düyməsi ilə burada saxla.</p><Link href="/products">Məhsullara bax</Link></div>}</main>}
