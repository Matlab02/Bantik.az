"use client";
import { FormEvent, useState } from "react";
const empty={name:"",phone:"",email:"",city:"Bakı",address:""};
export function LocalProfile({ mode }: { mode: "profile" | "address" }) { const key=`bantik-${mode}`; const [saved,setSaved]=useState(false); const [data,setData]=useState(()=>{if(typeof window==="undefined")return empty;try{return JSON.parse(localStorage.getItem(key)||"")||empty}catch{return empty}}); function submit(e:FormEvent){e.preventDefault();localStorage.setItem(key,JSON.stringify(data));setSaved(true);} return <form className="account-form" onSubmit={submit}>{mode==="profile"?<><label>Ad və soyad<input required value={data.name} onChange={e=>setData({...data,name:e.target.value})}/></label><label>Telefon<input inputMode="tel" required value={data.phone} onChange={e=>setData({...data,phone:e.target.value})}/></label><label>E-poçt<input type="email" value={data.email} onChange={e=>setData({...data,email:e.target.value})}/></label></>:<><label>Şəhər<input required value={data.city} onChange={e=>setData({...data,city:e.target.value})}/></label><label>Ünvan<textarea required value={data.address} onChange={e=>setData({...data,address:e.target.value})}/></label></>}<button>Yadda saxla</button>{saved&&<p>Məlumat yadda saxlanıldı.</p>}</form> }
